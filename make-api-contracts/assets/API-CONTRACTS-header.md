# API Contracts

> GENERATED by `make-api-contracts` from `docs/product`. **Do not hand-edit this file
> or `openapi.yaml` by hand** -- change the feature specs / flows and re-run
> `/make-api-contracts`. (Human-owned operation fields -- `summary`, `description`,
> `x-notes` -- are preserved across a rebuild.)

This is the human index of the project's **own** API surface. The machine source of
truth is [`openapi.yaml`](./openapi.yaml) (OpenAPI 3.1). External providers (Stripe,
Shopify, ...) are referenced by ADR/integration, not redefined here.

**Run a mock server against it** -- zero conversion:

```
npx @stoplight/prism-cli mock docs/product/api/openapi.yaml     # Stoplight Prism
# or point Mockoon / Microcks / any OpenAPI-native mock at the same file
```

Each operation traces back to the requirement(s) it satisfies (`x-trace-req`), the
feature it belongs to (`x-feature` / tag), the version it was born from
(`x-source-version`), and its governing decision (`x-trace-adr`).
